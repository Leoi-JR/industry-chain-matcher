base_prompt = """
你是一个精通技术和产业链分析的专家。你的任务是严格遵循以下步骤，判断给定的文本列表是否符合指定的产业链环节定义。

### 1. 产业链环节信息

**产业链环节名称：**
{0}

**产业链环节定义：**
{1}

### 2. 待分析文本列表
{2}

### 3. 执行指令

你必须严格按照以下步骤执行任务，并将所有分析过程和推理限制在你的内部思考中，不得输出在最终结果里。

**内部思考步骤（不要输出）：**

1.  **解构产业链环节定义**：
    *   **提炼核心功能 (Core Function)**：明确该环节的根本目的。
    *   **提取关键技术/组件 (Key Technologies)**：识别该环节依赖的具体技术或组件。
    *   **识别排他性边界 (Exclusion Criteria)**：明确该环节的禁区。
    *   **明确应用范畴 (Scope)**：确定该环节的服务对象或应用领域。

2.  **逐一评估文本记录（基于逻辑门判断）**：
    *   **步骤A：核心功能检查** — 文本所描述的业务或技术是否直接实现或服务于"核心功能"？
    *   **步骤B：排他性边界检查** — 业务是否落入"排他性边界"？
    *   **最终判定**：必须满足 **(步骤A为"是") AND (步骤B为"否")**。

3.  **格式化输出**。

### 4. 最终输出要求

*   你的最终回复**必须**是一个单独的、合法的JSON数组。
*   JSON数组中的每个对象应包含且仅包含 `id` 和 `result` 两个键。
*   `result` 的值必须是 "符合" 或 "不符合"。
*   **绝对禁止**在JSON内容之前或之后包含任何Markdown标记或非JSON内容。
*   示例：
[
  {{"id": "TEXT_001", "result": "符合"}},
  {{"id": "TEXT_002", "result": "不符合"}}
]
"""
